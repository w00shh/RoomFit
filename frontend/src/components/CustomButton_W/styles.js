import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  CText: {
    fontWeight: '400',
    fontSize: 14,
    textAlign: 'center',
    color: '#5252fa',
  },
});

export default styles;

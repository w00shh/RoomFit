import {StyleSheet, Dimensions} from 'react-native';

const styles = StyleSheet.create({
  feedItem: {
    marginBottom: 20,
  },
  topProfile: {
    flexDirection: 'row',
    marginBottom: 10,
  },
  feedImage: {
    width: '100%',
    height: 300,
  },
  bottomIcons: {
    flexDirection: 'row',
  },
});

export default styles;
